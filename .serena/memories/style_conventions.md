# Style & conventions
- Language: C# (.NET)
- Naming: PascalCase for types/methods/properties; camelCase for locals/parameters.
- Files: one type per file (typical), with namespaces matching folder structure.
- Records are used for data models in NodeEditor.Blazor/Models.
- Tests use xUnit style.
