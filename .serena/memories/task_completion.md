# Task completion checklist
- Ensure new code compiles (dotnet build).
- Run relevant tests (dotnet test NodeEditor.Blazor.Tests\NodeEditor.Blazor.Tests.csproj).
- Keep UI types out of persistence DTOs per migration plan.
- Update docs if behavior or architecture changes.
